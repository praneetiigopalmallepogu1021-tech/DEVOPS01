name: Simple CI Pipeline

on:
  push:
    branches: [ "master" ]


  pull_request:
    branches: ["master"]       

jobs:
  build:
    runs-on: ubuntu-latest


    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Run a simple command
      run: echo "This is my automated pipeline run 1"
              
    - name: step1
      run: echo "This is step1"
      
    - name: step2
      run: echo "This is step2"
     
    - name: breaking pipeline
      run: echo "this pipeline is fixed"  
    
    - name: Build Docker Image
      run: docker build -t devops01